# Findings & Decisions: Repository Architecture Audit

## Requirements
- Review the repository for redundant code, unused code, and extractable modules.
- Judge with an architect-level standard: maintainability, boundaries, coupling, testability, and refactor risk.
- Prefer evidence-backed recommendations over speculative rewrites.

## Research Findings
- Repository is frontend-heavy: `git ls-files` line count across TS/Vue/Python/CSS is about 79k lines.
- The largest frontend files are `frontend/src/editor/nodes/NodeCard.vue` at 5,383 lines, `frontend/src/editor/canvas/EditorCanvas.vue` at 4,672 lines, and `frontend/src/editor/workspace/EditorWorkspaceShell.vue` at 2,873 lines. These are likely the highest leverage architecture review targets.
- The largest backend runtime files are `backend/app/tools/model_provider_client.py` at 1,380 lines, `backend/app/core/runtime/node_system_executor.py` at 1,226 lines, and `backend/app/core/langgraph/runtime.py` at 1,041 lines.
- Root scripts are minimal: root `package.json` delegates only to `scripts/start.mjs`; frontend has build/dev/preview only. This reduces orchestration ambiguity.
- Frontend dependencies are intentionally small: Vue, Vue Router, Pinia, Vue I18n, Element Plus, and icons. Most complexity is application code, not dependency sprawl.
- Backend dependencies are also small and expected for this product: FastAPI, Pydantic, LangGraph, OpenAI, httpx, YAML, BeautifulSoup, multipart upload.
- No tracked `__pycache__`, `.pyc`, `dist`, or `.dev_*` files were found.
- Static unused-code check with `vue-tsc --noUnusedLocals --noUnusedParameters` found a small set of truly unused frontend symbols. Safe cleanup was applied in `NodeCard.vue`, `WorkspaceSearchField.vue`, and `graph-node-creation.ts`.
- Removed `scripts/lm_core1.py`: it was 1,989 lines, unreferenced by tracked scripts/frontend/backend/docs/README, and conflicted with the current OpenAI-compatible provider migration strategy already expressed by `lm_core0.py`, `download_Gemma_gguf.py`, and `lm-server`.
- `NodeCard.vue` mixes node chrome, state-port editing, text editing, runtime controls, skill picker, input value editor, output preview, condition editing, port drag-reorder, and CSS in one component. This is the most urgent frontend extraction candidate.
- `EditorCanvas.vue` mixes rendering, viewport, pan/zoom/pinch, minimap coordination, node drag/resize, anchor measurement, connection snapping/completion, edge editing popovers, lock handling, and run presentation. It should be split by interaction model rather than by visual section.
- `EditorWorkspaceShell.vue` mixes tab persistence, draft persistence, route sync, run polling/SSE, graph mutation forwarding, state panel actions, creation menu actions, import/export, and user feedback. It is a shell in name but currently owns too much application orchestration.
- Backend runtime concentration is similar: `model_provider_client.py`, `node_system_executor.py`, and `core/langgraph/runtime.py` each contain multiple layers of responsibilities that should be separated only after behavior tests cover the boundaries.

## Candidate Areas To Inspect
- Frontend graph editor: `frontend/src/editor/canvas`, `frontend/src/editor/nodes`, `frontend/src/editor/workspace`.
- Frontend pages/models/API wrappers: `frontend/src/pages`, `frontend/src/api`, `frontend/src/lib`.
- Backend runtime and graph execution: `backend/app/core`, `backend/app/api`, `backend/app/skills`.
- Scripts and dev orchestration: `scripts`, root `package.json`, `frontend/package.json`, backend requirements.

## Technical Decisions
| Decision | Rationale |
|----------|-----------|
| Do not start by deleting code | Static signals alone can be misleading in a UI-heavy codebase with dynamic routing and tests. |
| Prioritize modules with high size and high coupling | Those create the most maintenance drag and refactor risk. |

## Issues Encountered
| Issue | Resolution |
|-------|------------|
| `vue-tsc` unused check failed on stale handlers and imports | Removed the unused symbols and updated the structure test that asserted the previous handler shape. |

## Final Recommendations
- Next engineering slice should be `NodeCard.vue` extraction, starting with floating panel state and port reorder logic.
- The second slice should be `EditorCanvas.vue` connection/measurement extraction.
- The third slice should be `EditorWorkspaceShell.vue` run stream and draft persistence extraction.
- Backend refactor should begin with `model_provider_client.py` because its protocol boundaries are clearer than executor/runtime semantics.
- Avoid a broad rewrite; move pure functions first, keep public APIs as thin compatibility facades, and preserve current tests during each extraction.

## 2026-04-28 Cleanup Execution Findings
- Current cleanup should begin with `NodeCard.vue`, matching the roadmap's P1 recommendation.
- The safest immediate slice is the agent port reorder logic: preview ordering, target index calculation, selector escaping, source rect extraction, and floating pill style are pure or nearly pure and currently live inside the 5k-line component.
- `actionPopoverStyle`, `stateEditorPopoverStyle`, and `agentAddPopoverStyle` are identical transparent Element Plus popover style objects in `NodeCard.vue`; they can share one constant while preserving the public template bindings.
- Existing structure tests assert the previous in-component helper layout, so they must be updated alongside the extraction to verify the new module boundary instead of locking duplication in place.
- Editing existing state fields has a clean pure-model boundary: schema-to-draft conversion, immutable field updates, anchor-key extraction, and update-patch construction can live outside `NodeCard.vue` while the component keeps lock guards, popover confirmation state, translated errors, and emits.
- The production large chunk warning was primarily caused by synchronous page imports and unsplit vendor dependencies. Route-level dynamic imports reduce the app entry chunk from 1,674.16 kB to about 131 kB, while stable Vue and Element Plus vendor chunks keep routing behavior unchanged and avoid noisy circular manual chunk output.
- Agent runtime input helpers have a small model boundary in `agentConfigModel.ts`: thinking-mode normalization and temperature input parsing can be tested as pure functions while `NodeCard.vue` retains lock checks, emits, and select focus handling.
- Uploaded asset display text has a pure model boundary in `uploadedAssetModel.ts`: labels, summaries, text previews, and empty-state descriptions can be tested outside the component while `NodeCard.vue` keeps file selection, drop handling, error reporting, and graph state emits.
- Knowledge-base input presentation has a separate pure boundary in `inputKnowledgeBaseModel.ts`: option labels, unavailable-current-value handling, and selected-description fallback can move out of `NodeCard.vue` while the component keeps select events and state updates.
- Output-node advanced configuration has a shared pure boundary in `outputConfigModel.ts`: display/persist option lists, labels, active checks, and simple config patches can be reused by `NodeCard.vue` and `nodeCardViewModel.ts` instead of duplicated.
- Condition-rule editing has a pure draft boundary in `conditionRuleEditorModel.ts`: value draft normalization, operator patching, value patch no-op detection, and exists-operator disabled state can move out of `NodeCard.vue` while the component keeps DOM event handling and emits.
- Condition loop-limit editing has a pure draft and commit boundary in `conditionLoopLimit.ts`: draft formatting, invalid-value reset, no-op detection, and patch creation can move out of `NodeCard.vue` while the component keeps DOM event handling, blur behavior, lock guards, and emits.
- Agent skill picker behavior has a final small pure boundary in `skillPickerModel.ts`: attach/remove no-op detection and skills patch creation can move out of `NodeCard.vue` while the component keeps picker visibility, lock guards, and emits.
- EditorCanvas route handle presentation has a pure model boundary in `routeHandleModel.ts`: branch tone/palette, flow-out hotspot geometry, and route handle CSS variables can move out of `EditorCanvas.vue` while the component keeps DOM events, connection state, and pointer interactions.
- EditorCanvas interaction styling has a pure model boundary in `canvasInteractionStyleModel.ts`: color alpha conversion, connection preview strokes, projected edge CSS variables, flow hotspot geometry, and state anchor connection variables can move out while `EditorCanvas.vue` keeps active connection state, eligibility sets, pointer handling, and graph emits.
- EditorCanvas node presentation has a pure model boundary in `canvasNodePresentationModel.ts`: transform styles, node card size CSS variables, fallback rendered dimensions, minimap node models, and minimap run-state mapping can move out while `EditorCanvas.vue` keeps DOM measurement, resize drag state, viewport updates, and graph emits.
- EditorCanvas data-edge state editing has a pure model boundary in `canvasDataEdgeStateModel.ts`: data-edge ids, floating confirm/editor positions, data-edge matching, and confirm/request-to-editor projections can move out while `EditorCanvas.vue` keeps timers, lock guards, emits, selection state, and request watchers. The canvas can also reuse `stateEditorModel.ts` for state draft construction, immutable draft edits, and update patches instead of duplicating the NodeCard state editor logic.
- EditorCanvas connection preview has a pure model boundary in `canvasConnectionModel.ts`: concrete state key detection, pending connection creation and identity, active source anchor lookup, preview state key resolution, accent color selection, and preview path model construction can move out while `EditorCanvas.vue` keeps pointer events, auto-snapping, connection completion, node-creation menu payloads, and graph mutation emits.
- EditorCanvas still had several small pure helpers after the connection extraction. Condition route target labels now fit `conditionRouteTargetsModel.ts`; human-review/run-node wrapper presentation now fits `canvasRunPresentationModel.ts`; flow/route delete confirmation target, style, active-state, and emit-action projection now fit `flowEdgeDeleteModel.ts`. `EditorCanvas.vue` keeps live refs, timers, lock guards, selected-edge updates, and actual emits.
- EditorCanvas has a broad pure-helper cleanup seam across minimap edge projection, forced-visible edge ids, pending state previews, virtual create port visibility/transient anchors, selected reconnect projection, keyboard edge delete projection, data-edge disconnect payloads, pinch math, and viewport display formatting. Extracting those helpers reduced the component from 4,226 to 4,039 lines while preserving side-effect ownership in the Vue component.
- Repeated production builds now complete without a Vite large chunk warning. The warning is no longer a current blocker; remaining bundle work should be treated as targeted performance work rather than cleanup-risk mitigation.
- Baseline comparison showed the state auto-snap regression came from filtering pending agent input sources through concrete-only state keys, which dropped `VIRTUAL_ANY_OUTPUT_STATE_KEY`. Preserving virtual output sources restores auto-snapping from a virtual output into an agent create-input target.
- EditorCanvas connection interaction has a larger pure boundary than the earlier small connection preview model: node-body auto-snap selection, concrete state row hit selection, virtual create-input fallback anchors, reverse virtual output fallback anchors, and node-creation menu payload construction can live in `canvasConnectionInteractionModel.ts`.
- New node creation context is sensitive to preserving virtual state keys even when their value type resolves to `null`; dropping those keys removes the downstream naming/binding context for created nodes.
- Edge overlay interactions in `EditorCanvas.vue` have a composable boundary around refs, timers, state-editor requests, emitted delete/disconnect/update actions, and missing-edge cleanup. Keeping only pointer-to-canvas coordinate conversion in the component preserves the DOM event surface while moving behavior into `useCanvasEdgeInteractions.ts`.
- Node measurement state has a separate composable boundary: node element registration, resize/mutation observers, rendered anchor-slot measurement, flow anchor fallback measurement, and measured-size cache cleanup can live in `useCanvasNodeMeasurements.ts`. The component still owns canvas viewport math and graph interaction decisions.
- NodeCard title/description editing has a safe composable boundary in `useNodeCardTextEditor.ts`: trigger pointer tracking, confirm timers, focus scheduling, drafts, trim/no-op metadata patches, and close/commit behavior can move out while `NodeCard.vue` keeps popup template markup, locked-edit handling, peer panel cleanup, and the `update-node-metadata` emit payload.
- The formal roadmap document for this cleanup is `docs/future/2026-04-28-architecture-refactor-roadmap.md`; root `task_plan.md`, `findings.md`, and `progress.md` are execution memory and must stay aligned with that roadmap.
- The first roadmap `useNodeFloatingPanels` step can proceed safely in smaller sub-slices. Top-action confirm timers and global outside-panel close listeners are now isolated in `useNodeFloatingPanels.ts`; state-editor confirm/delete confirm can follow after their behavior is covered by focused controller tests.
- `useNodeFloatingPanels.ts` can also safely own state editor and remove-state confirm anchor/timer refs. `NodeCard.vue` should still keep state draft synchronization, actual state update emits, and remove-port-state emits until those flows have stronger controller coverage.
- The roadmap `usePortReorder` step has a clean composable boundary around pointer state, global pointer listeners, target-index resolution through the existing pure model, floating pill projection, release-time reorder emits, and click suppression after drag. `NodeCard.vue` should keep only the side-effect callbacks: locked interaction guard, state-editor cleanup on activation, state-editor click forwarding, and the existing `reorder-port-state` event contract.
- `StatePortList.vue` can safely own agent real state port row markup, `+ input`/`+ output` create entry rows, create-popover wiring, and local scoped styles as long as `NodeCard.vue` keeps state draft mutation, validation, locked-edit guards, and graph mutation emits. Because `NodeCard.vue` uses scoped CSS, moving markup into a child requires moving the corresponding row/create styles with it; otherwise child internals lose the port pill visual treatment.
- `AgentSkillPicker.vue` is a safe first node-body subcomponent extraction because it owns presentation-only skill picker markup and styles while `NodeCard.vue` keeps the skill model derivation, locked interaction guard, attach/remove patch decisions, and agent config emit behavior.
- `AgentRuntimeControls.vue` is a safe second node-body subcomponent extraction because it owns the model select, thinking select card, breakpoint switch card, model-select collapse ref, and local runtime-control styles while `NodeCard.vue` keeps model option derivation, refresh-model emit, lock guards, and config/breakpoint emits.
- `AgentNodeBody.vue` can safely own the agent body presentation wrapper around port lists, runtime controls, skill picker, and prompt textarea if `NodeCard.vue` keeps state derivation, draft mutation, validation, lock guards, skill patch creation, and graph mutation emits. Scoped styles do not cross the component boundary, so textarea surface styles must move with the textarea.
- `InputNodeBody.vue` can safely own the input boundary segmented control, knowledge-base selector, upload/dropzone/preview surface, editable textarea, read-only surface, and input-specific scoped styles while `NodeCard.vue` keeps input value derivation, uploaded asset parsing, knowledge-base option derivation, lock guards, file/drop handlers, and state/config mutation emits. Keeping the primary output state pill as a parent-owned slot reduces risk around state editor/create popover behavior.
- `OutputNodeBody.vue` can safely own output persistence control presentation, preview metadata, rendered plain/markdown/json preview surfaces, and output-specific scoped styles while `NodeCard.vue` keeps preview content derivation, display/persist option derivation, lock guards, output config handlers, and graph mutation emits. Keeping the primary input state pill as a parent-owned slot avoids widening state editor/create popover risk.
- Phase 28 completed the larger NodeCard presentation closeout: `ConditionNodeBody.vue` now owns condition source pill presentation, state/create popover wiring, operator/value/loop controls, and local condition styles while `NodeCard.vue` keeps draft synchronization, lock guards, and emits; `NodeCardTopActions.vue` now owns the top action dock and advanced/confirm popover presentation while `NodeCard.vue` keeps action confirmation, config patch, and runtime mutation handlers.
- After Phase 28, `NodeCard.vue`'s remaining high-value P1 surface is the primary input/output state-pill slot presentation and residual chrome around state editor/create popover hosting. Title/description refs should remain parent-owned unless the text editor composable is adapted with stronger behavior coverage.
- Phase 29 moved primary input/output state-pill popover presentation into `PrimaryStatePort.vue` and the port-reorder floating drag preview into `FloatingStatePortPill.vue`. This is the last large low-risk P1 presentation boundary found in `NodeCard.vue`; remaining parent-owned state draft sync, validation, confirmation, lock guards, and graph/state emits are intentionally orchestration-heavy and should only move behind stronger behavior/controller coverage.
- Phase 30 confirms the low-risk `NodeCard.vue` P1 gate is effectively closed and starts P2 Canvas work with `canvasNodeDragResizeModel.ts`. Node drag/resize threshold checks, viewport-scale projection, rounding, and resize result projection are pure enough to test outside `EditorCanvas.vue`; pointer capture, animation-frame scheduling, connection completion, and update emits should remain component-owned until the next controller/composable slice is covered.
- Phase 31 pairs the pure `canvasNodeDragResizeModel.ts` helpers with `useCanvasNodeDragResize.ts`. The composable can safely own node drag/resize refs, pointer capture release, scheduled update dispatch, residual click suppression, and teardown, while `EditorCanvas.vue` keeps selection, active connection cleanup, auto-snap, connection completion, panning, DOM measurement, and graph mutation emits.
- Phase 32 adds `useCanvasConnectionInteraction.ts` as the first connection controller slice. Pending connection refs, pending preview point, auto-snapped target ref, connection-hover node ref, pending start/toggle from anchors, preview point updates, and hover-change notifications can move out safely; auto-snap target selection, node creation payloads, connection completion emit mapping, and graph mutation emits should stay at the current component/model boundary until separate focused tests cover them.
- Phase 33 adds `canvasConnectionCompletionModel.ts` for connection completion action projection. The mapping from active connection plus target anchor to `connect-flow`, `connect-route`, `connect-state`, `connect-state-input-source`, `reconnect-flow`, or `reconnect-route` is pure and testable; the actual Vue `emit` dispatch should remain in `EditorCanvas.vue` as a typed switch because Vue emit overloads do not accept a union event name cleanly.
- Phase 34 extends `canvasConnectionInteractionModel.ts` with high-level auto-snap target resolution. Flow hotspot selection, node-body fallback, reverse state-input concrete row selection, state-output concrete row selection, and create-input fallback selection can be resolved from pure inputs once `EditorCanvas.vue` supplies `nodeIdAtPointer` and `canvasPoint`; DOM hit-testing and pointer coordinate conversion should remain component-owned.
- Phase 35 confirms the connection request layer is a safe next P2 boundary. `canvasConnectionInteractionModel.ts` can own pending connection creation-menu request projection, including payload preservation for virtual state keys and successful-open cleanup policy; `canvasConnectionCompletionModel.ts` can own completion request projection, including the current no-op-action behavior that still clears connection interaction state. `EditorCanvas.vue` should continue to own DOM event inputs, Vue `emit` dispatch, and imperative cleanup execution.
- Phase 36 confirms active connection pointer-up routing is another safe model boundary. The decision order `locked cleanup` before `auto-snapped completion` before `open creation menu` is now explicit and testable in `canvasConnectionInteractionModel.ts`, while `EditorCanvas.vue` keeps pointer capture/release, actual completion/menu calls, node drag/resize finish, and pan teardown.
- Phase 37 confirms active-connection node pointer-down routing can also move into the interaction model. The model can decide between node-body snap completion and continuing normal node pointer-down behavior, and can carry the current `preventDefault`/canvas-focus policy as booleans while `EditorCanvas.vue` still executes the DOM side effects and completion call.
- Phase 38 confirms active-connection pointer-move preview request projection is safe to model. `canvasConnectionInteractionModel.ts` now owns the request shape that combines hover node, target anchor, and fallback point; `EditorCanvas.vue` still owns DOM node hit-testing, pointer-to-canvas conversion, auto-snap resolver inputs, RAF scheduling, and preview state mutation execution.
- Phase 39 confirms anchor pointer-down routing is safe to model as long as setup policy stays explicit. The model now decides locked-edit, direct completion, ignored anchor, and start/toggle connection cases, while `EditorCanvas.vue` still executes DOM focus, transient cleanup, selection, window text-selection clearing, pending connection start/toggle, selected-edge cleanup, and actual completion.
- Phase 40 confirms node-resize pointer-down routing belongs with the drag/resize model. `canvasNodeDragResizeModel.ts` can decide missing node, locked edit, active connection block, and start-resize setup policy; `EditorCanvas.vue` should continue to execute DOM focus, pointer capture, transient cleanup, selected-edge cleanup, rendered-size lookup, and `startNodeResizeDrag`.
- Phase 41 confirms node pointer-down drag setup can move into `canvasNodeDragResizeModel.ts` if active-connection completion remains separately resolved through `canvasConnectionInteractionModel.ts`. The drag/resize model can own missing-node, locked-edit, inline-editor focus preservation, and start-drag setup policy; `EditorCanvas.vue` should continue to execute DOM focus/preventDefault, pointer capture, pending connection cleanup, selected-edge cleanup, selection mutation, and `startNodeDrag`.
- Phase 42 confirms canvas pointer-down pan/pinch setup can live with the pinch model. `canvasPinchZoomModel.ts` can decide whether the post-touch-registration pointer-down should run the pinch cleanup path or the pan startup path; `EditorCanvas.vue` should keep pointer snapshot storage, `buildPinchZoomStart` execution, DOM focus/preventDefault, pointer capture, transient cleanup, selection clearing, and `viewport.beginPan`.
- Phase 43 confirms wheel zoom request projection is a safe viewport interaction model boundary. `canvasViewportInteractionModel.ts` can own zero-delta ignore, direction-to-scale delta, pointer-centered zoom request construction, and no-rect set-scale fallback; `EditorCanvas.vue` should keep DOM rect lookup and actual viewport mutation.
- Phase 44 confirms empty-canvas double-click creation routing is safe to model next to the connection creation-menu decisions. `canvasConnectionInteractionModel.ts` can own locked edit, ignored target, and open-menu action selection; `EditorCanvas.vue` should keep DOM target inspection, canvas coordinate conversion, and the actual `open-node-creation-menu` emit.
- Phase 45 confirms file-drop node creation routing fits the same interaction model boundary. `canvasConnectionInteractionModel.ts` can own locked edit, ignored target, missing file, and create-from-file payload decisions; `EditorCanvas.vue` should keep DOM target inspection, `dataTransfer` access, canvas coordinate conversion, drop-effect mutation, and the actual `create-node-from-file` emit.
- Phase 46 confirms drag-over drop-effect selection is another safe micro-boundary. `canvasConnectionInteractionModel.ts` can own locked edit and dragged-file availability mapping to `copy` or `none`; `EditorCanvas.vue` should keep the actual `dataTransfer.dropEffect` mutation and dragover event binding.
- Phase 47 confirms selected-edge keyboard delete routing belongs with the flow-edge delete model. `flowEdgeDeleteModel.ts` can own editable-target ignore, locked-edit action, missing-edge ignore, non-deletable-edge ignore, and flow/route delete payload projection; `EditorCanvas.vue` should keep `preventDefault`, locked guard side effects, emits, selected-edge clearing, and pending connection point cleanup.
- Phase 48 confirms edge visibility toolbar click routing belongs with `edgeVisibilityModel.ts`. The model can own locked edit, same-mode no-op, and mode-change cleanup policy; `EditorCanvas.vue` should keep the toolbar event binding, locked guard side effects, edge mode ref mutation, and cleanup execution.
- Phase 49 confirms lock-banner click routing fits `canvasRunPresentationModel.ts`. The model can decide between `locked-edit-attempt` and `open-human-review` from the current run node id; `EditorCanvas.vue` should keep the button event binding and actual emits.
- Phase 50 confirms locked-node pointer capture routing fits a dedicated `canvasLockedInteractionModel.ts`. The model can own unlocked no-op versus locked capture policy, including whether a locked-attempt notification should emit, while `EditorCanvas.vue` keeps DOM target classification, event side effects, focus, cleanup, selection, and graph interaction refs.
- Phase 51 confirms the generic locked-canvas guard fits the same `canvasLockedInteractionModel.ts` boundary. The model can own allow/block and cleanup policy flags, while `EditorCanvas.vue` should keep the imperative cleanup, selected-edge mutation, and locked-attempt emit.
- Phase 52 confirms edge pointer-down routing is a safe dedicated model boundary. `canvasEdgePointerInteractionModel.ts` can own locked edit, flow/route delete-confirm, data-edge state-confirm, and selected-edge fallback policy while `EditorCanvas.vue` keeps actual focus, cleanup, confirm composable calls, pending-point updates, selection clearing, and emits.
- Phase 53 confirms pending-connection creation-menu routing belongs with `canvasConnectionInteractionModel.ts`. The model can own locked/no-connection ignore, open-menu payload, and cleanup policy while `EditorCanvas.vue` keeps canvas point resolution, event coordinates, actual menu emit, and cleanup execution.
- Phase 54 confirms connection-completion execution routing belongs with `canvasConnectionCompletionModel.ts`. The model can own locked/no-connection ignore plus completion cleanup policy, while `EditorCanvas.vue` keeps actual typed Vue emit dispatch, connection cleanup execution, selected-edge cleanup, and graph mutation behavior.
- Phase 55 confirms zoom button scale/reset routing belongs with `canvasViewportInteractionModel.ts`. The model can own zoom-out/zoom-in clamped scale projection and reset viewport action selection, while `EditorCanvas.vue` keeps canvas-center zoom execution, viewport mutation, wheel zoom execution, and viewport draft emits.
- Phase 56 confirms minimap center-view routing belongs with `minimapModel.ts`. The model can own empty canvas-size ignore and centered viewport projection, while `EditorCanvas.vue` keeps canvas size refresh, actual viewport mutation, canvas focus execution, and minimap event binding.
- Phase 57 confirms external node-focus viewport routing belongs with `focusNodeViewport.ts`. The model can own missing-target ignore and focused viewport projection, while `EditorCanvas.vue` keeps node lookup, DOM rect/element measurement, actual selection mutation, and viewport mutation.
- Phase 58 confirms pinch-zoom update routing belongs with `canvasPinchZoomModel.ts`. The model can own missing pinch/target cleanup, non-positive distance ignore, center calculation, and `zoomAt` request projection, while `EditorCanvas.vue` keeps active pointer cache updates, DOM canvas rect lookup, actual cleanup execution, and viewport mutation.
